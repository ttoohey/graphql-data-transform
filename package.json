{
  "name": "graphql-data-transform",
  "version": "1.0.4",
  "description": "Transform data objects based on GraphQL schema",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir dist",
    "clean": "rm -rf dist",
    "prepublish": "npm run clean && npm test && npm run build"
  },
  "author": "Gency Digital <hello@gency.com.au>",
  "homepage": "https://github.com/ttoohey/graphql-data-transform",
  "bugs": "https://github.com/ttoohey/graphql-data-transform/issues",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/node": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "babel-core": "^6.26.3",
    "babel-preset-minify": "^0.5.0",
    "graphql": "^15.4.0",
    "graphql-tag": "^2.10.1",
    "jest": "^26.6.3",
    "prettier": "^2.2.1"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "minify"
    ]
  }
}
